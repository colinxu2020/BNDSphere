[project]
name = "bndsphere"
version = "0.1.0"
description = "bnds club manage platform"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.1",
    "alembic>=1.18.4",
    "fastapi[standard]>=0.135.1",
    "mariadb>=1.1.14",
    "passlib[argon2]>=1.7.4",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.13.1",
    "pyjwt>=2.12.1",
    "sqlalchemy>=2.0.48",
    "uvicorn[standard]>=0.42.0",
]

[dependency-groups]
dev = []

[tool.mypy]
strict = true

[tool.ruff.lint]
select =["ALL"]
extend-ignore = ['EM101', 'TRY003', 'D107', 'D102', 'D103', 'TC001', 'D101', 'D100', 'D104', 'TC002', 'TC003']

[tool.ruff]
extend-exclude = ["**/migrations"]
