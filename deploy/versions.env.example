# Deployment image pins for THIS host, and the record of what is deployed.
#
# On first deployment:  cp deploy/versions.env.example deploy/versions.env
#
# After that the deploy workflow owns deploy/versions.env and rewrites it
# atomically. Do not hand-edit it while a deploy is in flight.
#
# This file is the single record of the deployed version: it is what Compose
# will start, so it cannot drift from what is running. Before overwriting it,
# a deploy copies it to deploy/versions.env.prev — that copy IS the rollback
# target, and a successful rollback deletes it.
#
# A deployed host holds pins written after loading a release tarball, e.g.
#   APP_VERSION=v1.2.3
#   BACKEND_IMAGE=bndsphere-backend:v1.2.3

APP_VERSION=local
BACKEND_IMAGE=bndsphere-backend
CADDY_IMAGE=bndsphere-caddy
